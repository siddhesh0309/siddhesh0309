import subprocess

# Define paths
rbwm_src = os.path.abspath("rbwm")              # local rbwm folder created by your script
rbs_dst = r"T:\RBS"                              # your network/shared final folder

# Robocopy command (multithreaded, fast, silent)
cmd = f'robocopy "{rbwm_src}" "{rbs_dst}" /E /Z /MT:32 /R:1 /W:1 /COPY:D /NDL /NFL /NP /LOG:NUL'
print("🚀 Moving rbwm PDFs to final destination using robocopy...")
subprocess.run(cmd, shell=True)
print("✅ rbwm PDFs moved to RBS successfully.")
