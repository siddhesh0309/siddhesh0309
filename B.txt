month_key = start.strftime('%b-%Y')  # e.g., Jul-2025
config = config_df[
    (config_df['Customer Name'].str.lower() == customer.lower()) &
    (config_df['Month'].str.lower() == month_key.lower())
]

if config.empty:
    print(f"Skipping {customer} for {month_key}: no config found.")
    continue

config = config.iloc[0]
tbill, spread, tenor, roi = config['TBill Rate'], config['Spread'], config['Tenor'], config['ROI']
